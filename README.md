# Book Library Viewer

A small Flask web application that displays a library's book catalog (stored
in a Python dictionary) as a styled HTML table, along with quick stats on
total, available, and checked-out books.

## Project Structure